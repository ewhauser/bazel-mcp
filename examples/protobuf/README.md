# protobuf reducer examples

This standalone workspace pins protobuf 35.1 and uses its real `proto_library`
descriptor-generation action. `//:success` is valid. The manual cases cover
protoc syntax, missing-import, and undefined-message diagnostics.
