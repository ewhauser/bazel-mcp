# TypeScript and JavaScript reducer examples

This dependency island pins Aspect `rules_js`, a hermetic Node 24.12.0
toolchain, and the TypeScript 5.9.3 compiler archive. `//:success` is a working
JavaScript binary. The manual cases run real `tsc` and Node processes for
TypeScript compilation, JavaScript syntax, and JavaScript runtime failures.
