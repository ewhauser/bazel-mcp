# Python reducer examples

This standalone workspace pins `rules_python` 2.2.0 and its hermetic Python
3.12 toolchain. `//:success` is a working executable. The manual test targets
under `//cases` produce real interpreter syntax, import, and traceback output
for the MCP recorder.
