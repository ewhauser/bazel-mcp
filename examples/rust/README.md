# Rust reducer examples

This standalone workspace pins `rules_rust` and Rust 1.96.1. `//:success` is a
working binary. The manual test case emits real libtest assertion and panic
evidence, including the source location and bounded assertion details used by
the Rust test reducer.
